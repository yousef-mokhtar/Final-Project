# پروژه بک‌اند فروشگاه کاستومی

این پروژه، سرویس بک‌اند برای یک پلتفرم فروشگاه اینترنتی چند-فروشندگی است که با Django و Django REST Framework توسعه یافته است.

## ✨ ویژگی‌های کلیدی

-   **سیستم کاربران:** احراز هویت کامل با JWT، پروفایل کاربری، و ورود دو مرحله‌ای با OTP.
-   **سیستم چند-فروشندگی:** کاربران می‌توانند به فروشنده تبدیل شده و فروشگاه، آدرس‌ها و محصولات خود را مدیریت کنند.
-   **کاتالوگ محصولات:** مدیریت دسته‌بندی‌ها، محصولات و گالری تصاویر.
-   **فرآیند خرید:** پیاده‌سازی کامل سبد خرید، اعمال تخفیف و فرآیند Checkout.
-   **پرداخت:** اتصال به درگاه پرداخت (زرین‌پال) و مدیریت وضعیت پرداخت.
-   **سیستم نظرات:** قابلیت ثبت امتیاز و نظر برای محصولات.
-   **پنل ادمین پیشرفته:** پنل مدیریت کامل با قابلیت‌های پیشرفته مانند اکشن‌های گروهی و نمایش `inline`.
-   **مستندات API:** مستندات تعاملی و خودکار با Swagger UI.
-   **زیرساخت مدرن:** پروژه به طور کامل با Docker کانتینریزه شده و از Celery و Redis برای کارهای ناهمزمان (مثل ارسال ایمیل) استفاده می‌کند.

---

## 🚀 راه‌اندازی سریع پروژه (با Docker)

این روش پیشنهادی برای اجرای پروژه است و تمام نیازمندی‌ها (دیتابیس، Redis و ...) را به صورت خودکار مدیریت می‌کند.

### پیش‌نیازها
-   [Docker](https://www.docker.com/products/docker-desktop/) و Docker Compose باید روی سیستم شما نصب و در حال اجرا باشند.

### مراحل راه‌اندازی

۱. **کلون کردن پروژه** و وارد شدن به پوشه آن.

۲. **ایجاد فایل `.env`:**
   یک فایل به نام `.env` در ریشه پروژه بسازید و محتوای زیر را در آن کپی کنید. **مقادیر محرمانه را با اطلاعات خودتان جایگزین کنید.**

   ```env
   # کلید امنیتی جنگو - یک مقدار تصادفی و طولانی وارد کنید
   SECRET_KEY='django-insecure-your-secret-key-here'
   DEBUG=True

   # تنظیمات دیتابیس PostgreSQL برای Docker
   DB_NAME=customie_db
   DB_USER=customie_user
   DB_PASS=a_strong_password_for_db # یک رمز عبور قوی برای دیتابیس انتخاب کنید
   DB_HOST=db
   DB_PORT=5432

   # تنظیمات Redis برای Docker
   REDIS_HOST=redis
   REDIS_PORT=6379

   # تنظیمات ارسال ایمیل (مثال با Gmail)
   # برای این کار باید یک "App Password" از حساب گوگل خود بسازید
   EMAIL_HOST_USER=your_email@gmail.com
   EMAIL_HOST_PASSWORD=your_gmail_app_password

   # کد مرچنت زرین‌پال
   ZARINPAL_MERCHANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
   ```

۳. **ساخت و اجرای کانتینرها:**
   این دستور تمام سرویس‌ها را در پس‌زمینه (detached mode) اجرا می‌کند.
   ```bash
   docker-compose up --build -d
   ```

۴. **اجرای Migration ها:**
   برای ساخت جداول در دیتابیس PostgreSQL، دستور زیر را اجرا کنید:
   ```bash
   docker-compose exec web python manage.py migrate
   ```

۵. **ایجاد کاربر ادمین:**
   برای دسترسی به پنل ادمین، یک کاربر ادمین بسازید:
   ```bash
   docker-compose exec web python manage.py createsuperuser
   ```

**تبریک! پروژه با موفقیت راه‌اندازی شد.**

-   **API سرور:** `http://localhost:8000/`
-   **پنل ادمین:** `http://localhost:8000/admin/`
-   **مستندات API (Swagger):** `http://localhost:8000/api/schema/swagger-ui/`

---

### 🧪 اجرای تست‌ها

برای اجرای تمام تست‌های خودکار پروژه در محیط داکر، از دستور زیر استفاده کنید:
```bash
docker-compose exec web python manage.py test
```

### توقف پروژه

برای متوقف کردن تمام سرویس‌ها:
```bash
docker-compose down
```